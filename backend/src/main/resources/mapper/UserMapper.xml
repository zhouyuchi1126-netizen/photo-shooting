<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.photoshoot.mapper.UserMapper">

    <resultMap id="userResultMap" type="com.example.photoshoot.model.User">
        <id property="id" column="id" />
        <result property="username" column="username" />
        <result property="password" column="password" />
        <result property="displayName" column="display_name" />
        <result property="role" column="role" />
        <result property="wechatOpenid" column="wechat_openid" />
    </resultMap>

    <select id="selectByUsername" resultMap="userResultMap" parameterType="string">
        SELECT id, username, password, display_name, role, wechat_openid
        FROM ps_user
        WHERE username = #{username}
    </select>

    <select id="selectByWechatOpenid" resultMap="userResultMap" parameterType="string">
        SELECT id, username, password, display_name, role, wechat_openid
        FROM ps_user
        WHERE wechat_openid = #{wechatOpenid}
    </select>

    <insert id="insertUser" parameterType="com.example.photoshoot.model.User">
        INSERT INTO ps_user (id, username, password, display_name, role, wechat_openid)
        VALUES (#{id}, #{username}, #{password}, #{displayName}, #{role}, #{wechatOpenid})
    </insert>

    <update id="updateWechatOpenid" parameterType="com.example.photoshoot.model.User">
        UPDATE ps_user SET wechat_openid = #{wechatOpenid} WHERE id = #{id}
    </update>
</mapper>
